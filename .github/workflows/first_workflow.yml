name: first workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: first_step
      run: echo "First Step ..."
    - name: secound_step
      run: echo "Secound step ..."
    - name: multi_line_command
      run: |
        ls -la
        whoami
        pwd
